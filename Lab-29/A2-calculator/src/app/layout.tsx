export const metadata = {
  title: 'Calculator App',
  description: 'NextJS Basic Calculator',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
